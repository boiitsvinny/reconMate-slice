# Docker Compose defaults. Copy to .env before running Docker Compose if needed.
POSTGRES_DB=reconmate
POSTGRES_USER=reconmate
POSTGRES_PASSWORD=change-me-for-local-development
POSTGRES_PORT=5432
API_PORT=8000
WEB_PORT=3000
